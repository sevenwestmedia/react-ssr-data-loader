{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Debug test: Current file",
            "program": "${workspaceRoot}/node_modules/.bin/jest",
            "args": ["${relativeFile}"],
            "cwd": "${workspaceRoot}",
            "sourceMaps": true
        }
    ]
}
